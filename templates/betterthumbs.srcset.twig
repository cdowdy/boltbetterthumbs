{% import 'macros/srcset.macros.twig' as attribs %}
{% if sourceExists %}
{{ attribs.srcset( srcset) }}
{% else %}
    <img src="{{ notFoundImg }}"
         width="{{ notFoundSize }}"
         alt="image not found">
{% endif %}