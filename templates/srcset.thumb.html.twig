{% import './macros/srcset.macros.twig' as attribs %}
{% if sourceExists %}
<img {{ attribs.buildImg( sizes, widthDensity, classes, id, dataAttributes, srcset, srcImg, altText) }} >
{% else %}
<img src="{{ notFoundImg }}"
     width="{{ notFoundSize }}"
     alt="image not found">
{% endif %}