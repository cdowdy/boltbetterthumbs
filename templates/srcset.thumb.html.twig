{% import 'srcset.macros.twig' as attribs %}
{% if sourceExists %}
<img {{ attribs.buildImg( sizes, autosizes, widthDensity, classes, id, dataAttributes, srcset, srcImg, lazyload, lazyPattern, altText ) }} >
{% else %}
<img src="{{ notFoundImg }}"
     width="{{ notFoundSize }}"
     alt="image not found">
{% endif %}